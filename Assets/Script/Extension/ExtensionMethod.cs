using UnityEngine;
using System.Collections;

public class ExtensionMethod : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	static class InstantiateExtension
	{
		/*
		public static Object InstantiateNotAddCloneName(GameObject target)
		{
			GameObject InstantiateObj = Instantiate(target, target.transform.localPosition, target.transform.localRotation) as GameObject;	
			InstantiateObj.name = InstantiateObj.name.Split(InstantiateObj.name.Substring(InstantiateObj.name.Length -3));	
		}
		*/
	}
	
}
